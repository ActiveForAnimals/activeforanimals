{#
/**
 * @file
 * Organization filter overview.
 *
 */
#}
<div class="section">
  <div class="list">
    <div class="title">
      {{ content.title }}
    </div>
    <div class="content">
      <ul>
        {% if content.filters is empty %}
          {{ content.empty }}
        {% else %}
        {% for filter in content.filters %}
        <li>
          <div class="column-wrapper">
            <div class="column-10-15 first-column">
              {{ filter.title }}
            </div>
            <div class="column-5-15 last-column">
              {{ filter.more_info }}
            </div>
          </div>
        </li>
        {% endfor %}
        {% endif %}
      </ul>
    </div>
    <div class="footer">
      {{ content.pager }}
      {{ content.create_filter_link }}
    </div>
  </div>
</div>

{#
/**
 * @file
 * Organization event template overview.
 *
 */
#}
<div class="section">
  <div class="list">
    <div class="title">
      {{ content.title }}
    </div>
    <div class="content">
      <ul>
        {% if content.event_templates is empty %}
          {{ content.empty }}
        {% else %}
        {% for event_template in content.event_templates %}
        <li>
          <div class="column-wrapper">
            <div class="column-10-15 first-column">
              {{ event_template.title }}
            </div>
            <div class="column-5-15 last-column">
              {{ event_template.more_info }}
            </div>
          </div>
        </li>
        {% endfor %}
        {% endif %}
      </ul>
    </div>
    <div class="footer">
      {{ content.pager }}
      {{ content.create_event_template_link }}
    </div>
  </div>
</div>
